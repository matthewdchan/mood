import React from 'react';
import './Header.css';

function Header() {
    return ( 
        <header> 
            <h1>mood</h1>
            <button>Add New Entry</button>
        </header>
    );
};

export default Header;